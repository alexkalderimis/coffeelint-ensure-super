{
  "name": "coffeelint-ensure-super",
  "version": "0.0.2",
  "description": "Ensure super is called in certain methods.",
  "main": "index.js",
  "scripts": {
    "pretest": "npm run compile",
    "test": "mocha --require should --compilers coffee:coffee-script/register",
    "prepublish": "npm test",
    "compile": "coffee -c index.coffee"
  },
  "keywords": [
    "coffeelint", "coffeelintrule", "coffee-lint", "static-analysis"
  ],
  "author": "Alex Kalderimis",
  "license": "MIT",
  "devDependencies": {
    "coffeelint": "^1.8.1",
    "coffee-script": "^1.8.0",
    "mocha": "^2.1.0",
    "should": "^4.4.2"
  },
  "dependencies": {
  }
}
